#include <stdio.h>
#define max 100
int main(void)
{
	int n;
	printf("1<=N<=100 정수 N을 입력하시오:\n");
	scanf("%d", &n);

	char num[max];
	int sum = 0;
	printf("N개의 정수를 입력하시오:\n");
	scanf("%s", num);

	for (int i = 0; i < n; i++) {
		sum = sum + num[i]-'0';
	}
	printf("%d", sum);
}
