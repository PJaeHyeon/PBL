#include <stdio.h>

int main() { //문제 번호 2739번
	int n; //필요한 변수 설정
	

	printf("구구단 n단의 n을 입력하세요."); 
	scanf("%d", &n);

	for (int i = 1; i < 10; i++) { //구구단을 구하기 위한 for문 설정
		printf("%d * %d = %d \n", n , i,n*i);
	}
}
